# No third-party dependencies — schema/auth/services use only the Python
# standard library (sqlite3, hashlib, secrets, zoneinfo) by design, so the
# database layer has zero install footprint on the till/dashboard machine.
